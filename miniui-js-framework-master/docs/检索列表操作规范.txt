规范如下：

1.检索列表只有在用户主动触发检索的时候才刷新；

2.修改详细信息后刷新详细信息部分，如有必要也需同步更新检索列表中对应的记录；

3.删除当前记录后，自动定位到检索列表的下一行记录。最后一行仍旧在最后一行，如果没有数据，则清空详细信息界面的内容；

4.新增记录后，新增的记录添加到检索列表（根据各自界面的要求确定加到第一行还是最后一行），详细信息界面显示新增的记录信息。

5.如果界面操作在检索区域存在流程（例如：药品养护界面，新增完后点击养护开始，左侧检索区域状态更改为养护开始；点击养护结束，左侧检索区域状态更改为养护结束）